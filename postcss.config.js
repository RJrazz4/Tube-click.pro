// PostCSS pipeline: Tailwind CSS for utility generation and Autoprefixer
// for cross-browser vendor prefixes. Mirrors Vite's default PostCSS setup.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
